# Development Environment Configuration

environment: dev
debug: true

# API
api_host: "0.0.0.0"
api_port: 8000
api_debug: true

# Logging
log_level: DEBUG
log_format: text

# Performance
enable_cache: true
cache_ttl: 3600
rate_limit_per_minute: 100
